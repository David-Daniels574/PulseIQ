fastapi
uvicorn[standard]
pydantic
transformers
torch
nltk
googlemaps
python-dotenv
google-generativeai
sqlalchemy
psycopg2-binary
alembic
requests
beautifulsoup4
xgboost
pandas
scikit-learn
chromadb
langchain
langchain-core
langchain-google-genai

# ── New dependencies ───────────────────────────────────────────────
 
# Browser automation / scraping helpers
selenium                  # headless browser fallback
webdriver-manager         # auto-downloads the correct ChromeDriver
python-dateutil           # robust date parsing ("2 months ago" → datetime)
lxml                      # faster HTML parser for BeautifulSoup
 
# Confidence engine / MECE clustering
# scikit-learn already listed above (TF-IDF + cosine similarity)
 
# PDF export (boardroom report)
reportlab                 # PDF generation
Pillow                    # image handling inside PDFs
 
# Alembic for DB migrations (already listed but pinning here for clarity)
alembic